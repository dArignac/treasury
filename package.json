{
  "name": "treasury",
  "scripts": {
    "emulators:live": "firebase emulators:start",
    "emulators:demo": "firebase --project demo-treasury emulators:start",
    "test": "firebase --project demo-treasury emulators:exec 'cd tests && npm test'"
  }
}
